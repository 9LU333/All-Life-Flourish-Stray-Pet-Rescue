spring:
    application:
        name: PetRescueProgram
    datasource:
        url: jdbc:mysql://localhost:3306/petrescueprogram?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
        driver-class-name: com.mysql.cj.jdbc.Driver
        username: root
        password: Liguangyuan123
    jackson:
        date-format: yyyy-MM-dd
        time-zone: GMT+8

mybatis-plus:
    configuration:
        log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
